# 보안 정책 (Security Policy)

이 문서는 본 저장소의 보안 정책과 취약점 신고 절차를 안내합니다.

## 지원 버전 (Supported Versions)

| 버전 | 보안 업데이트 지원 |
| ---- | ------------------ |
| main / chatbot (최신) | ✅ |
| 그 외 브랜치 | ❌ |

최신 기본 브랜치와 활성 개발 브랜치에 대해서만 보안 패치를 제공합니다.

## 취약점 신고 (Reporting a Vulnerability)

보안 취약점을 발견하면 **공개 이슈(Issue)로 올리지 말고** 비공개로 알려주세요.

- 우선 방법: GitHub **Security → Report a vulnerability**(Private vulnerability reporting)로 신고
- 대체 방법: 저장소 관리자에게 직접 연락 (연락처: `TODO: 보안 담당자 이메일 기입`)

신고 시 아래 내용을 포함하면 처리가 빨라집니다.
- 영향 범위와 재현 절차
- 관련 로그·요청 예시 (민감정보는 제거)
- 예상 심각도

접수 후 영업일 기준 3일 이내에 1차 회신, 확인된 취약점은 우선순위에 따라 수정합니다.

## 자격증명 및 비밀정보 관리 (Secrets & Credentials)

- `.env`, `*.pem`, `*.key` 등 비밀정보 파일은 **절대 커밋하지 않습니다.** (`.gitignore`로 차단)
- AWS 접근은 장기 액세스 키 대신 **IAM 역할(EC2 Instance Profile)** 사용을 우선합니다.
- 부득이하게 액세스 키를 쓰는 경우, 최소 권한 원칙을 적용하고 사용 후 즉시 비활성화/삭제합니다.
- 비밀정보가 실수로 커밋되면 즉시 **키를 폐기(rotate)** 하고 히스토리에서 제거합니다.

## 네트워크 노출 (Network Exposure)

- 보안 그룹(Security Group)은 **최소 개방 원칙**을 따릅니다. 필요한 포트만, 가능한 한 좁은 소스 대역으로 엽니다.
- 애플리케이션 포트를 `0.0.0.0/0`(전체 공개)으로 여는 것은 지양하고, 필요한 IP 대역 또는 리버스 프록시(HTTPS) 뒤에 둡니다.
- 관리용 포트(SSH 22 등)는 신뢰된 IP로만 제한합니다.

## 애플리케이션 접근 제어 (Application Access Control)

- 외부에 노출되는 엔드포인트는 인증(기본 인증, 토큰, 또는 프록시 레벨 인증)으로 보호합니다.
- 특히 쓰기/AI 호출을 유발하는 엔드포인트(예: `POST /notices`)는 인증 없이 공개하지 않습니다. (비용 및 오남용 방지)
- 입력값은 서버에서 검증하고, 오류 메시지에 민감정보를 노출하지 않습니다.

## 의존성 관리 (Dependencies)

- 의존성은 정기적으로 점검하고 알려진 취약점은 신속히 업데이트합니다. (`npm audit` 등)
- 새 의존성 추가 시 신뢰할 수 있는 패키지인지 확인합니다.

## 컨테이너 (Containers)

- 이미지에는 소스와 런타임 의존성만 포함하고, 비밀정보·키 파일은 넣지 않습니다.
- 자격증명은 이미지에 굽지 않고 런타임(환경변수 또는 IAM 역할)으로 주입합니다.
